const browserFactory = require('./browserFactory')
const pageFactory = require('./pageFactory')
const sessionFactories = require('./sessionFactories')
const userFactory = require('./userFactory')


module.exports = {
    browserFactory,
    pageFactory,
    sessionFactories,
    userFactory
}